# 📊 数据库结构映射说明

## 🔍 你的 Notion 数据库实际结构

根据检查结果，你的数据库有以下属性：

| 属性名称 | 类型 | 用途 | 映射到代码 |
|---------|------|------|------------|
| 标题 | title | 文章标题 | `title` |
| 期数 | rich_text | 文章描述/期数 | `description` |
| 类型 | select | 文章分类 | `category` |
| 标签 | multi_select | 文章标签 | `tags` |
| 封面 | files | 文章封面图片 | `image` |
| 评分 | number | 文章评分 | `views` |
| 热门资源 | checkbox | 是否热门 | `likes` |
| 创建时间 | created_time | 创建时间 | `date` |
| 网盘1/网盘2 | url | 下载链接 | `url` |
| 格式 | multi_select | 文件格式 | 未使用 |
| 上次编辑时间 | last_edited_time | 编辑时间 | 未使用 |

## 🔧 代码映射逻辑

### 标题和描述
- **标题** → `title`：直接使用
- **期数** → `description`：作为文章描述

### 分类和标签
- **类型** → `category`：选择类型，如"视频"、"平面"、"三维"等
- **标签** → `tags`：多选类型，包含丰富的标签选项

### 媒体和统计
- **封面** → `image`：支持文件和外部链接
- **评分** → `views`：数字类型，作为浏览量显示
- **热门资源** → `likes`：复选框，热门资源显示100个赞

### 时间和链接
- **创建时间** → `date`：用于排序和显示
- **网盘1/网盘2** → `url`：优先使用网盘1，备用网盘2

## 📝 数据库优化建议

### 1. 添加缺失属性（可选）
如果你想让代码更完整，可以添加：
- **Description**：专门的描述字段
- **Status**：发布状态（Published/Draft）
- **Views**：专门的浏览量字段
- **Likes**：专门的点赞数字段
- **Comments**：评论数字段

### 2. 保持现有结构
当前结构已经足够使用，代码已经适配了你的实际属性名称。

## 🚀 使用方法

1. **确保数据库已共享**：与你的 Notion 集成共享
2. **重启服务**：`npm run dev`
3. **检查数据**：页面应该能正常显示你的资源

## 🔍 验证映射

运行以下命令验证映射是否正确：
```bash
npm run check:db    # 检查数据库属性
npm run test:notion # 测试数据获取
npm run dev         # 启动服务查看效果
```

## 💡 注意事项

- 所有属性名称都使用中文，确保与你的数据库完全匹配
- 代码会自动处理缺失的属性，使用默认值
- 如果数据库结构发生变化，需要相应更新代码映射
